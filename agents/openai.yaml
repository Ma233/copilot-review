interface:
  display_name: "Copilot Review"
  short_description: "Triage Copilot review comments and apply valid fixes."
  default_prompt: "Use $copilot-review to fetch the latest Copilot PR review, triage comments as apply/verify/ignore, and implement the apply items."
